import * as ApplyPositionContainerStyles from "./Container.style";

export const ApplyPositionContainer = ({ children }: { children: React.ReactNode }) => {
    return <ApplyPositionContainerStyles.Container>{children}</ApplyPositionContainerStyles.Container>;
};
