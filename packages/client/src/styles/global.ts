import { css } from "@emotion/react";

export const globalStyle = css`
    *,
    *::before,
    *::after {
        margin: 0;
        padding: 0;
        border: 0;

        box-sizing: border-box;
    }
`;
